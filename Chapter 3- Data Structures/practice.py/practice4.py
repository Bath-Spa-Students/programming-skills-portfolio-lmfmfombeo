#. Assume the lists numbers1 has 100 elements and numbers2 is an empty list. Write code that
#copies the values in numbers to numbers2.

# Assuming numbers1 has 100 elements
numbers1 = [1, 2, 3, ...]

# Creating an empty list numbers2
numbers2 = []

# Copying the values from numbers1 to numbers2
numbers2 = numbers1.copy()
print(numbers2)

